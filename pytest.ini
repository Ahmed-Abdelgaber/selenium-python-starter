[pytest]
minversion = 8.0
addopts = -ra --strict-markers --disable-warnings
testpaths = src/tests
markers =
    smoke: quick checks
    regression: broader coverage
log_cli = true
log_cli_level = INFO
